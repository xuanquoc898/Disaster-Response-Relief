<UserControl x:Class="D2R.Views.UserControls.AddDonorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Khu vực thêm mạnh thường quân -->
        <GroupBox Header="Thêm Mạnh Thường Quân" FontWeight="Bold" Margin="0,0,5,0" FontSize="20" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="Họ tên:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Margin="5" Width="400"/>

                <Label Content="CCCD:" Grid.Row="1" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="txtCCCD" Grid.Row="1" Grid.Column="1" Margin="5" Width="400"/>

                <Label Content="SĐT:" Grid.Row="2" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="txtPhone" Grid.Row="2" Grid.Column="1" Margin="5" Width="400"/>

                <Label Content="Email:" Grid.Row="3" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="txtEmail" Grid.Row="3" Grid.Column="1" Margin="5" Width="400"/>

                <Button Content="Thêm Mạnh Thường Quân"
                        Width="250"
                        Height="40"
                        FontSize="16"
                        HorizontalAlignment="Left"
                        Margin="5"
                        Grid.Row="5" Grid.ColumnSpan="2"
                        Style="{StaticResource MaterialDesignRaisedButton}" Click="Button_Click"/>
            </Grid>
        </GroupBox>

        <!-- Danh sách Mạnh Thường Quân -->
        <GroupBox Header="Danh sách Mạnh Thường Quân"
                  FontWeight="Bold" FontSize="20"
                  Margin="0,10,5,0"
                  Grid.Row="1">
            <DataGrid x:Name="dgDonors"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="10"
                      FontSize="16"
                      AlternatingRowBackground="#EEE"
                      HeadersVisibility="Column"
                      RowHeaderWidth="0"
                      IsReadOnly="True"
                      Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="3*"/>
                    <DataGridTextColumn Header="CCCD" Binding="{Binding CCCD}" Width="3*"/>
                    <DataGridTextColumn Header="Số điện thoại" Binding="{Binding Phone}" Width="2*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="3*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>