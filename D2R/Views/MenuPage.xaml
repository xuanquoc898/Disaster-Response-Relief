<Page x:Class="D2R.Views.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:D2R.Views"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="270"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Grid.Column="0" Background="#912D24" VerticalAlignment="Stretch">
            <TextBlock Text="MENU" FontSize="20" FontWeight="Bold" Foreground="White" Margin="16"/>
            <!-- Icons Menu -->
            <Button Content="🏠 Trang chủ" Style="{StaticResource MaterialDesignFlatButton}" Margin="10"/>
            <Button Content="👥 Quản lí người dùng" Style="{StaticResource MaterialDesignFlatButton }" Margin="10"/>
            <Button Content="🤝 Quản lí mạnh thường quân" Click="DonorManagement_Click"  Margin="10"/>
            <Button Content="🏪 Quản lí kho hàng cứu trợ" Style="{StaticResource MaterialDesignFlatButton}" Margin="10"/>
            <Button Content="🚑 Quản lí chiến dịch cứu trợ" Style="{StaticResource MaterialDesignFlatButton}" Margin="10"/>
            <Button Content="📈 Báo cáo và thống kê" Style="{StaticResource MaterialDesignFlatButton}" Margin="10"/>
        </StackPanel>

        <!-- Content bên phải -->
        <Grid Grid.Column="1" Background="#FAFAFA">
            <StackPanel Margin="40">
                <Border Background="#6E2F2C" CornerRadius="10" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Add a task that you need to finish today!" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBox materialDesign:HintAssist.Hint="Title" Margin="0,0,0,10"/>
                        <Button 
                            materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                            materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
                            materialDesign:ButtonProgressAssist.Value="-1"
                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                            Content="Add Task" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Border>
                <TextBlock Text="Disaster Response and Relief System" FontSize="18" FontWeight="Bold" Foreground="Red"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

