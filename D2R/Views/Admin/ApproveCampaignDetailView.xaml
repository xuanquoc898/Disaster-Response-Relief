<UserControl x:Class="D2R.Views.Admin.ApproveCampaignDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="500" Width="800"
             Background="White">
    <Grid Margin="20" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Thông tin chiến dịch -->
        <StackPanel Grid.Row="0" Margin="0 0 0 20">
            <TextBlock x:Name="CampaignNoteText" FontSize="18" FontWeight="Bold" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
                <TextBlock Text="Loại thiên tai: " FontWeight="SemiBold"/>
                <TextBlock x:Name="DisasterTypeText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 2 0 0">
                <TextBlock Text="Mức độ thiên tai: " FontWeight="SemiBold"/>
                <TextBlock x:Name="DisasterLevelText"/>
            </StackPanel>
        </StackPanel>

        <!-- Danh sách vật phẩm phân theo danh mục -->
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="CategoryPanel" />
        </ScrollViewer>

        <!-- Nút hành động -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button Content="Phân phối" Click="Approve_Click" Margin="5"/>
            <Button Content="Từ chối" Click="Reject_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
