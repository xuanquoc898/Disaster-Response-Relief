<UserControl x:Class="D2R.Views.UserControls.DonorManagerment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:D2R.Views.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Grid root sẽ co giãn đúng trong ContentControl của MainLayout -->
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Thanh công cụ -->
            <RowDefinition Height="*" />
            <!-- Danh sách -->
        </Grid.RowDefinitions>

        <!-- THANH TÌM KIẾM + THÊM MỚI -->
        <DockPanel Grid.Row="0" Margin="10">
            <TextBox Width="250"
                     Margin="0,0,10,0"
                     materialDesign:HintAssist.Hint="Tìm kiếm theo tên"
                     VerticalAlignment="Center"/>
            <Button Content="Tìm kiếm"
                    Style="{StaticResource MaterialDesignRaisedButton}" 
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"/>
            <DockPanel DockPanel.Dock="Right">
                <Button Content="Thêm Mới"
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Background="#391867" 
                        Foreground="White"
                        Margin="10,0" Click="Button_Click"
                        />
            </DockPanel>
        </DockPanel>

        <!-- DANH SÁCH -->
        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  IsReadOnly="True"
                  Margin="10"
                  SelectionMode="Single"
                  Background="White"
                  AlternatingRowBackground="#EEE">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="3*"/>
                <DataGridTextColumn Header="CCCD" Binding="{Binding Email}" Width="2*"/>
                <DataGridTextColumn Header="Số điện thoại" Binding="{Binding Phone}" Width="2*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Address}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>