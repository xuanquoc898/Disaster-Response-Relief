<UserControl x:Class="D2R.Views.Admin.PlannedCampaignListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid Margin="20" Background="White">
        <StackPanel>
            <TextBlock Text="Danh sách chiến dịch cần phân phối" FontSize="18" FontWeight="Bold" Margin="0 0 0 20"/>
            <DataGrid x:Name="CampaignGrid"
                  AutoGenerateColumns="False"
                  Height="350"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên chiến dịch" Binding="{Binding Note}" Width="*"/>
                    <DataGridTextColumn Header="Khu vực" Binding="{Binding Area.Name}" Width="200"/>
                    <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedDate, StringFormat=d}" Width="150"/>
                    <DataGridTemplateColumn Header="Thao tác" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Xem chi tiết" Tag="{Binding CampaignId}" Click="ViewDetail_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
