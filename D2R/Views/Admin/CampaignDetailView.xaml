<UserControl x:Class="D2R.Views.Admin.CampaignDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Chi tiết chiến dịch" FontSize="20" FontWeight="Bold" Margin="0 0 0 10"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tên chiến dịch: " FontWeight="SemiBold"/>
                <TextBlock x:Name="NoteText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Khu vực: " FontWeight="SemiBold"/>
                <TextBlock x:Name="AreaText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Loại thiên tai: " FontWeight="SemiBold"/>
                <TextBlock x:Name="DisasterTypeText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Mức độ thiên tai: " FontWeight="SemiBold"/>
                <TextBlock x:Name="DisasterLevelText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Ngày gửi yêu cầu: " FontWeight="SemiBold"/>
                <TextBlock x:Name="CreatedDateText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Ngày duyệt / từ chối: " FontWeight="SemiBold"/>
                <TextBlock x:Name="ApprovedOrRejectedDateText"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" x:Name="CompletedTimePanel" Visibility="Collapsed">
                <TextBlock Text="Thời gian xác nhận hoàn tất: " FontWeight="SemiBold"/>
                <TextBlock x:Name="CompletedTimeText"/>
            </StackPanel>

            <TextBlock Text="Danh sách vật phẩm yêu cầu" FontWeight="Bold" Margin="0 20 0 10"/>
            <ListView x:Name="ItemListView" Height="300">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Tên vật phẩm" Width="200" DisplayMemberBinding="{Binding ItemName}"/>
                        <GridViewColumn Header="Số lượng yêu cầu" Width="150" DisplayMemberBinding="{Binding QuantityRequested}"/>
                        <GridViewColumn Header="Danh mục" Width="150" DisplayMemberBinding="{Binding CategoryName}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</UserControl>
