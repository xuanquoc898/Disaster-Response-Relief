<UserControl x:Class="D2R.Views.UserManagermentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Quản lý người dùng" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <TabControl Grid.Row="1">
            <!-- Tab: Danh sách người dùng -->
            <TabItem Header="Danh sách tài khoản">
                <DockPanel Margin="5">
                    <!-- <Button x:Name="BtnRefresh" Content="Tải lại" Width="80" Click="BtnRefresh_Click" Margin="0,0,5,0" DockPanel.Dock="Top" /> -->

                    <DataGrid x:Name="DataGridUsers"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionChanged="DataGridUsers_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên tài khoản" Binding="{Binding Username}" Width="2*" />
                            <DataGridTextColumn Header="Vai trò" Binding="{Binding Role.RoleName}" Width="*" />
                            <DataGridTextColumn Header="Kho quản lý" Binding="{Binding Warehouse.Name}" Width="2*" />
                            <DataGridTextColumn Header="Vị trí kho" Binding="{Binding Warehouse.Area.Name}" Width="2*" />
                            <!-- Nút hành động ẩn trong dấu ... -->
                            <DataGridTemplateColumn Header="" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="..."
                                                Width="24" Height="24"
                                                Padding="0"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Foreground="Black"
                                                FontStretch="Medium"
                                                FontWeight="Bold"
                                                FontSize="16"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Click="RowActions_Click">
                                            <Button.ContextMenu>
                                                <ContextMenu DataContext="{Binding}">
                                                    <MenuItem Header="Reset mật khẩu" Click="RowResetPassword_Click" />
                                                    <MenuItem Header="Xóa" Click="RowDeleteUser_Click" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>

            <!-- Tab: Thêm người dùng mới -->
            <TabItem Header="Thêm người dùng">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Tên tài khoản:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
                    <TextBox x:Name="TxtUsername" Grid.Row="0" Grid.Column="1" Margin="5" />

                    <TextBlock Text="Mật khẩu:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
                    <PasswordBox x:Name="PwdPassword" Grid.Row="1" Grid.Column="1" Margin="5" />

                    <TextBlock Text="Vai trò:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
                    <ComboBox x:Name="CbxRole" Grid.Row="2" Grid.Column="1" Margin="5">
                        <ComboBoxItem Content="Admin" />
                        <ComboBoxItem Content="Staff" />
                    </ComboBox>

                    <TextBlock Text="Kho quản lý:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" />
                    <TextBox x:Name="TxtWarehouseName" Grid.Row="3" Grid.Column="1" Margin="5" />

                    <TextBlock Text="Vị trí kho:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" />
                    <TextBox x:Name="TxtWarehouseLocation" Grid.Row="4" Grid.Column="1" Margin="5" />

                    <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="BtnAddUser" Content="Thêm mới" Width="100" Click="BtnAddUser_Click" IsDefault="True" />
                        <Button x:Name="BtnClearForm" Content="Xóa form" Width="100" Margin="5,0,0,0" Click="BtnClearForm_Click" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>